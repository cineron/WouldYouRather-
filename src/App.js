import React from 'react';
import './App.css';

// This is just a placeholder for now! We'll build out the real UI
// (login, dashboard, poll details, leaderboard, etc.) in a later phase.
// Right now we're only focused on getting our Redux state & data foundation working.
function App() {
  return (
    <div className="App">
      <h1>Would You Rather</h1>
      <p>Phase 1 in progress: Redux store is wired up. UI coming soon!</p>
    </div>
  );
}

export default App;
